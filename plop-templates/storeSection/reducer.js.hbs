import nameSpaces from 'store/name-spaces';
import types from './types';

const initialState = {};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
}

export default reducer;
